{
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types src/play.ts",
    "docker": "docker build -t andromeda . && docker run --name game-of-life-ts andromeda"
  },
  "devDependencies": {
    "@types/node": "^24.9.2"
  }
}
